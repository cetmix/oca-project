# Installation Instructions

To install the module, follow these steps:

1. Download the module into your Odoo `addons` directory.
2. Ensure all dependencies specified in the `manifest.py` (e.g., `project_task_code` and `portal`) are installed.
3. Restart the Odoo server.
4. Activate Developer Mode, update the module list, and install **Project Task Code Portal**.
